{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-overlay",
  "description": "Narrow capabilities for the recording overlay window",
  "windows": ["recording_overlay"],
  "permissions": ["core:default"]
}
